---
group: 'changelog'
title: 'Version 2.5.0'
order: 1
slug: /changelog/2-5-0/
release: https://github.com/coengagedev/coengage/releases/tag/2.5.0
date: 'September 16th, 2021'
---

import {
  TimelineDemos,
  DialogDemos,
  ListDemos,
  ChipsDemos,
  PaginationDemos,
  TimeRangeInputDemos,
  RichTextEditorDemos,
} from 'coengageui_demos';

## UMD builds deprecation

All `coengageui_` packages will no longer default to umd builds via `browser` field in `package.json`.
This change will simplify tree shacking for Webpack and should not affect your Next.js, Gatsby, Vite or CRA applications.

## react-jss peer dependency removal

All `coengageui_` packages no longer require react-jss installation as peer dependency – it will be installed automatically as coengageui_core dependency.
Instead of direct react-jss usage coengageui_core now exports [createStyles](https://coengage.dev/theming/react-jss/) function
and a [set of components](https://coengage.dev/theming/next/) to implement server side rendering.

### createStyles function

New `createStyles` function lets you create styles with CoengageUI theme:

```tsx
import { createStyles } from 'coengageui_core';

const useStyles = createStyles((theme) => ({
  button: {
    backgroundColor: theme.colors.blue[6],
    color: theme.white,
    padding: `${theme.spacing.md}px ${theme.spacing.lg}px`,
  },
}));

function Demo() {
  const classes = useStyles();
  return <button className={classes.button}>My button</button>;
}
```

Note that it will only work if your application is wrapped with CoengageUIProvider.

### Builtin server side rendering

coengageui_core package now comes with components to simplify server side rendering setup with Next.js and any other environment.
Checkout [Next.js guide](https://coengage.dev/theming/next/) to learn more.

## New package coengageui_rte

[coengageui_rte](https://coengage.dev/others/rte/) is a new package with RichTextEditor component.
Component integrates seamlessly with your CoengageUITheme and provides all basic rich text editing features:

<Demo data={RichTextEditorDemos.usage} />

## New components

[Timeline](https://coengage.dev/core/timeline/) component lets you show list of events in chronological order:

<Demo data={TimelineDemos.usage} demoProps={{ toggle: true }} />

Navigate between multiple pages with new [Pagination](https://coengage.dev/core/pagination/) component:

<Demo data={PaginationDemos.configurator} />

[Chips](https://coengage.dev/core/chips/) component is an inline alternative to [RadioGroup](https://coengage.dev/core/radio-group/) and [MultiSelect](https://coengage.dev/core/multi-select/) components:

<Demo data={ChipsDemos.configurator} />

With [Dialog](https://coengage.dev/core/dialog/) component you can display fixed overlay at any side of the screen:

<Demo data={DialogDemos.usage} demoProps={{ toggle: true }} />

[List](https://coengage.dev/core/list/) component is a wrapper for ul and ol lists with option to replace bullets with icons:

<Demo data={ListDemos.icon} demoProps={{ toggle: true }} />

[TimeRangeInput](https://coengage.dev/dates/time-input/) component allows to capture time range from user:

<Demo data={TimeRangeInputDemos.usage} demoProps={{ toggle: true }} />

## New hooks

- [use-pagination](https://coengage.dev/hooks/use-pagination/) – manage pagination state
- [use-focus-return](https://coengage.dev/hooks/use-focus-return/) – capture last focused element on the page and return focus to it once condition is met

## New features

- [Button](https://coengage.dev/core/button/#white-variant) component now supports new `white` variant
- [Text](https://coengage.dev/core/text/#inherit-styles) now supports font styles inheritance from parent element with `inherit` prop
- [Paper](https://coengage.dev/core/paper/) component now supports `withBorder` prop to add 1px border
- [ActionIcon](https://coengage.dev/core/action-icon/) component now supports `loading` state (same as in [Button](https://coengage.dev/core/button/) component)
- [SegmentedControl](https://coengage.dev/core/segmented-control/) component now supports setting data as an array of strings `<SegmentedControl data={['React', 'Angular', 'Svelte', 'Vue']} />`
- [NumberInput](https://coengage.dev/core/number-input/#decimal-steps) component now supports decimal steps and precision
