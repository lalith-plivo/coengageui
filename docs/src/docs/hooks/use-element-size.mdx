---
group: 'coengageui-hooks'
package: 'coengageui_hooks'
category: 'dom'
title: 'use-element-size'
order: 1
slug: /hooks/use-element-size/
description: 'Get element width and height and subscribe to changes'
import: "import { useElementSize } from 'coengageui_hooks';"
docs: 'hooks/use-element-size.mdx'
source: 'coengageui-hooks/src/use-resize-observer/use-resize-observer.ts'
---

import { HooksDemos } from 'coengageui_demos';

## Usage

<Demo data={HooksDemos.useElementSizeDemo} />

## API

use-element-size is a simpler version of [use-resize-observer](https://coengage.dev/hooks/use-resize-observer/) hook.
Hook returns `ref` object that should be passed to the observed element, and the element's `height` and `width`.
On the first render (as well as during SSR), or when no element is being observed, `width` and `height` properties are equal to `0`.

```tsx
const { ref, width, height } = useElementSize();
```

## Definition

```tsx
function useElementSize<T extends HTMLElement = any>(): {
  MutableRefObject<T>,
  width: number,
  height: number
};
```
