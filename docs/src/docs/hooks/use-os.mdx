---
group: 'coengageui-hooks'
package: '@coengageui/hooks'
category: 'utils'
title: 'use-os'
order: 1
slug: /hooks/use-os/
description: 'Get user os'
import: "import { useOs } from '@coengageui/hooks';"
docs: 'hooks/use-os.mdx'
source: 'coengageui-hooks/src/use-os/use-os.ts'
---

import { HooksDemos } from '@coengageui/demos';

## Usage

use-os detects user's os. Possible values are: `undetermined`, `macos`, `ios`, `windows`, `android`, `linux`.
If os cannot be identified, for example, during server side rendering `undetermined` will be returned.

<Demo data={HooksDemos.useOsDemo} />

## TypeScript

You can import `OS` union type from `@coengageui/hooks`:

```tsx
import type { OS } from '@coengageui/hooks';
// OS type is 'undetermined' | 'macos' | 'ios' | 'windows' | 'android' | 'linux'
```

## Definition

```tsx
function getOS(): 'undetermined' | 'macos' | 'ios' | 'windows' | 'android' | 'linux';
```
